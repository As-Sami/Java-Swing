
import java.awt.Color;
import javax.swing.ImageIcon;
import javax.swing.JFrame;

public class MAIN{
	public static void main(String[] args) {
		
		SamFrame f = new SamFrame();


		// JFrame frame = new JFrame();
		// frame.setVisible(true);
		// frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // HIDE_ON_CLOSE, DO_NOTHING_ON_CLOSE

		// frame.setTitle("Sami's first GUI in java");
		// frame.setSize(600, 400); // width , height 
		// frame.setResizable(false); 

		// ImageIcon image = new ImageIcon("hiakymaru.jpg"); // crate an image
		// frame.setIconImage(image.getImage());
		// frame.getContentPane().setBackground(new Color(0x123456) ); // hex code... changing background
		//                                   new Color(12, 45 , 96) ); rgb
	}
}